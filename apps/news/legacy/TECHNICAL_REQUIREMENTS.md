# Техническое задание: Legal AI News Bot - Система персонализации и аналитики

**Версия:** 2.0
**Дата:** 2026-01-02
**Статус:** Планирование (код пишется только по команде)

---

## 📋 1. Общая задача

Расширить существующий Legal AI News Bot системой персонализации пользователей, интерактивной аналитикой и улучшенным UX для повышения вовлеченности аудитории.

**Текущая система:**
- ✅ Автоматический сбор новостей из 6 источников (Google News, Perplexity, RSS, Telegram)
- ✅ AI-анализ и генерация черновиков (GPT-4, Claude)
- ✅ Модерация через Telegram-бот и Mini App
- ✅ Публикация в канал @legal_ai_news
- ✅ Базовая аналитика (просмотры, реакции)

**Что добавляем:**
- 🆕 Профилирование пользователей и персонализация
- 🆕 Интерактивная система фидбека
- 🆕 CRM-аналитика и сегментация аудитории
- 🆕 AI-ассистент для работы с архивом новостей
- 🆕 Экспорт данных и автоматические отчеты

---

## 🎯 2. Функциональные требования

### 2.1. Онбординг и профилирование пользователей

**Описание:**
При первом запуске `/start` бот проводит интерактивный опрос для создания профиля пользователя.

**Вопросы онбординга:**

1. **Юридические интересы** (множественный выбор):
   - 📊 Персональные данные (GDPR, ПДн)
   - 🤖 Искусственный интеллект и право
   - 💼 Корпоративное право и M&A
   - 💰 Криптовалюты и блокчейн
   - 📝 Интеллектуальная собственность
   - ⚖️ Судебная практика
   - 🏛️ Регуляторика и комплаенс
   - 🌐 Международное право
   - 🔒 Кибербезопасность
   - 📈 Налоговое право

2. **Уровень экспертизы**:
   - 🎓 Студент юрфака
   - ⚖️ Практикующий юрист
   - 🏢 In-house юрист
   - 💼 Бизнес-лидер / CEO
   - 📰 Журналист / исследователь
   - 🤝 Просто интересуюсь темой

3. **Географические интересы**:
   - 🇷🇺 Россия
   - 🇪🇺 Европейский союз
   - 🇺🇸 США
   - 🇨🇳 Китай
   - 🌍 Глобально (все юрисдикции)

4. **Формат контента** (предпочтения):
   - ⚡ Краткие новости (2-3 абзаца)
   - 📄 Полный анализ (подробные статьи)
   - 📊 С инфографикой и статистикой
   - 🎯 Только с практическими выводами
   - 🔗 С первоисточниками и ссылками

5. **Частота уведомлений**:
   - 🔥 Мгновенно (каждая новая статья)
   - 📅 Дайджест 1 раз в день
   - 📬 Дайджест 2-3 раза в неделю
   - 🔕 Без уведомлений (читаю сам)

**Технические детали:**
- Диалог реализован через FSM (Finite State Machine) в aiogram
- Данные сохраняются в новую таблицу `user_profiles`
- Пользователь может обновить профиль через `/settings_profile`
- Пропуск онбординга: кнопка "Пропустить, заполню потом"

**Структура БД - таблица `user_profiles`:**
```sql
CREATE TABLE user_profiles (
    id SERIAL PRIMARY KEY,
    user_id BIGINT UNIQUE NOT NULL,
    username VARCHAR(255),
    first_name VARCHAR(255),

    -- Профиль интересов (JSONB)
    legal_interests JSONB DEFAULT '[]',  -- ['gdpr', 'ai_law', 'corporate']
    expertise_level VARCHAR(50),          -- 'student', 'lawyer', 'inhouse', 'business', 'journalist', 'enthusiast'
    geo_interests JSONB DEFAULT '[]',    -- ['ru', 'eu', 'us', 'global']
    content_format VARCHAR(50),           -- 'short', 'full', 'infographic', 'practical', 'sourced'
    notification_frequency VARCHAR(50),   -- 'instant', 'daily', 'weekly', 'off'

    -- Метаданные
    onboarding_completed BOOLEAN DEFAULT FALSE,
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW(),

    -- Статистика поведения
    total_reads INTEGER DEFAULT 0,
    total_reactions INTEGER DEFAULT 0,
    last_activity TIMESTAMP
);
```

---

### 2.2. Персонализация новостной ленты

**Описание:**
На основе профиля пользователя настраивается персональная лента новостей.

**Механизм работы:**

1. **Фильтрация по интересам:**
   - Новости тегируются при AI-анализе (тема, юрисдикция, сложность)
   - Пользователю показываются только релевантные его профилю новости
   - Остальные доступны через "Показать все"

2. **Ранжирование:**
   - Топ ленты = новости, соответствующие 100% интересам
   - Ниже = частичное совпадение (50%+)
   - Внизу = "Может быть интересно" (тематически близкие)

3. **Адаптивное обучение:**
   - Отслеживание, какие статьи читает пользователь
   - Корректировка профиля на основе поведения
   - "Вы часто читаете про GDPR - добавить в интересы?"

**Команды:**
- `/feed` - Персональная лента (только мои темы)
- `/feed_all` - Все новости
- `/feed_today` - Сегодняшний дайджест
- `/feed_weekly` - Недельный обзор

**Таблица тегирования статей - расширение `post_drafts`:**
```sql
ALTER TABLE post_drafts ADD COLUMN tags JSONB DEFAULT '{}';
-- Пример: {"topics": ["gdpr", "ai_law"], "jurisdictions": ["eu", "ru"], "complexity": "medium"}
```

---

### 2.3. Интерактивная система фидбека

**Описание:**
Собираем обратную связь от читателей для улучшения качества контента.

**Варианты фидбека:**

1. **На опубликованной статье (в канале):**
   - Inline-кнопки под постом: 👍 Полезно | 👎 Не то | 💾 Сохранить
   - При нажатии открывается мини-опрос в боте

2. **В личном боте:**
   - После прочтения статьи (tracking через UTM):
     ```
     Эта статья была полезна?
     ✅ Да, очень!
     👌 Скорее да
     😐 Так себе
     ❌ Нет, не то
     ```

3. **Детальный фидбек** (при негативной оценке):
   ```
   Что можно улучшить?
   • Слишком длинная
   • Не хватает деталей
   • Неактуальная тема
   • Плохой AI-анализ
   • Нерелевантный источник
   ```

4. **Предложения тем:**
   ```
   /suggest <тема>

   Пример: /suggest регулирование ChatGPT в финансовом секторе
   ```

**Структура БД - таблица `user_feedback`:**
```sql
CREATE TABLE user_feedback (
    id SERIAL PRIMARY KEY,
    user_id BIGINT NOT NULL,
    publication_id INTEGER REFERENCES publications(id),

    rating VARCHAR(20),  -- 'very_useful', 'useful', 'neutral', 'not_useful'
    issues JSONB,        -- ['too_long', 'lacks_details', 'irrelevant']
    comment TEXT,

    created_at TIMESTAMP DEFAULT NOW()
);
```

**Использование данных:**
- Автоматический отчет по quality score каждой статьи
- Коррекция AI-промптов на основе замечаний
- Черный список источников (если много негатива)

---

### 2.4. CRM-аналитика и сегментация

**Описание:**
Отслеживание поведения пользователей и сегментация аудитории.

**Метрики по пользователям:**

1. **Вовлеченность:**
   - Частота посещений бота
   - Количество прочитанных статей
   - Среднее время между визитами
   - Доля реакций (engagement rate)

2. **Предпочтения:**
   - Топ-3 темы по количеству прочтений
   - Любимые источники
   - Предпочитаемое время чтения

3. **Lifecycle стадии:**
   - **New** - только подписался (<3 дней, <5 прочтений)
   - **Active** - регулярно читает (7+ прочтений/неделю)
   - **Engaged** - читает + дает фидбек + рекомендует
   - **At Risk** - не заходил >7 дней
   - **Churned** - не заходил >30 дней

**Автоматические действия:**
- **At Risk → Реактивация:**
  Сообщение: "Вы давно не заходили! Вот что произошло за неделю: ..."

- **Engaged → VIP:**
  Предложение бета-тестирования новых функций

- **New → Онбординг:**
  Напоминание заполнить профиль через 3 дня

**Таблица БД - `user_analytics`:**
```sql
CREATE TABLE user_analytics (
    id SERIAL PRIMARY KEY,
    user_id BIGINT UNIQUE NOT NULL,

    -- Счетчики
    total_sessions INTEGER DEFAULT 0,
    total_articles_read INTEGER DEFAULT 0,
    total_reactions INTEGER DEFAULT 0,
    total_shares INTEGER DEFAULT 0,

    -- Поведение
    avg_session_duration INTEGER,        -- секунды
    favorite_topics JSONB DEFAULT '[]',  -- топ-3 темы
    favorite_sources JSONB DEFAULT '[]', -- топ-3 источника
    preferred_read_time TIME,            -- среднее время чтения

    -- Lifecycle
    lifecycle_stage VARCHAR(50),         -- 'new', 'active', 'engaged', 'at_risk', 'churned'
    first_seen TIMESTAMP,
    last_seen TIMESTAMP,
    last_reaction TIMESTAMP,

    -- Монетизация (будущее)
    is_premium BOOLEAN DEFAULT FALSE,
    ltv_estimate DECIMAL(10,2) DEFAULT 0,

    updated_at TIMESTAMP DEFAULT NOW()
);
```

**Команды для админа:**
- `/analytics_users` - Общая статистика по пользователям
- `/analytics_segments` - Распределение по сегментам
- `/analytics_churn` - Кто в зоне риска
- `/analytics_top_users` - Топ активных пользователей

---

### 2.5. AI-ассистент для работы с архивом

**Описание:**
Возможность задавать вопросы по всем опубликованным статьям через RAG (Retrieval-Augmented Generation).

**Функции:**

1. **Поиск по архиву:**
   ```
   /search GDPR штрафы 2025

   → Найдено 12 статей:
   1. "Meta оштрафовали на €1.2 млрд за GDPR" (12.05.2025)
   2. "Новые рекорды GDPR-штрафов в Q1 2025" (23.03.2025)
   ...
   ```

2. **Вопросы к AI:**
   ```
   /ask Какие основные изменения в регулировании ИИ произошли в ЕС за последний год?

   → [AI генерирует ответ на основе опубликованных статей с цитатами и ссылками]
   ```

3. **Сравнительный анализ:**
   ```
   /compare GDPR vs CCPA

   → [AI сравнивает регулирование на основе статей в архиве]
   ```

4. **Дайджесты:**
   ```
   /digest тема=ai_law период=месяц

   → Автоматический дайджест по теме за период
   ```

**Технология:**
- **Vector DB:** pgvector в PostgreSQL для эмбеддингов статей
- **Embeddings:** OpenAI text-embedding-3-small
- **LLM:** GPT-4 или Claude для генерации ответов
- **Context window:** топ-5 релевантных статей

**Таблица БД - `article_embeddings`:**
```sql
CREATE TABLE article_embeddings (
    id SERIAL PRIMARY KEY,
    publication_id INTEGER REFERENCES publications(id),

    -- Векторное представление
    content_embedding vector(1536),  -- OpenAI embedding
    title_embedding vector(1536),

    -- Метаданные для поиска
    text_content TEXT,               -- полный текст для контекста
    created_at TIMESTAMP DEFAULT NOW(),

    -- Индекс для векторного поиска
    INDEX ON content_embedding USING ivfflat (content_embedding vector_cosine_ops)
);
```

---

### 2.6. Экспорт данных и отчеты

**Описание:**
Автоматическая генерация отчетов и экспорт данных для анализа.

**Виды отчетов:**

1. **Еженедельный отчет (автоматически по понедельникам):**
   ```
   📊 Отчет за неделю 23.12.2025 - 29.12.2025

   📰 Контент:
   - Опубликовано: 23 статьи
   - Источники: Google News (12), Telegram (7), Perplexity (4)
   - Средний quality score: 8.4/10

   👥 Аудитория:
   - Активных пользователей: 234 (+12%)
   - Новых подписок: 18
   - Churn rate: 2.1%

   📈 Вовлеченность:
   - Просмотры: 4,521 (+8%)
   - Реакции: 287 (+15%)
   - Engagement rate: 6.3%

   🏆 Топ-3 статьи:
   1. "ЕС принял AI Act..." - 456 просмотров
   2. "OpenAI запустил..." - 398 просмотров
   3. "GDPR штрафы..." - 342 просмотра
   ```

2. **Месячный Executive Summary:**
   - Детальная аналитика за месяц
   - Тренды и insights
   - Рекомендации по улучшению

3. **Экспорт данных:**
   ```
   /export_stats период=месяц формат=excel

   → Генерирует Excel-файл с листами:
   - Publications (все статьи)
   - User Analytics (поведение пользователей)
   - Source Performance (эффективность источников)
   - Engagement Metrics (вовлеченность)
   ```

4. **Дашборд для админа:**
   - Расширение Mini App: раздел "Аналитика" с графиками
   - Real-time метрики
   - Воронки конверсии

**Технические детали:**
- Отчеты генерируются через Celery task (async)
- Excel-экспорт через `openpyxl`
- Графики через `matplotlib` или `plotly`
- Отправка через Telegram Bot API (document)

---

## 🏗️ 3. Архитектура системы

### 3.1. Новые компоненты

```
app/
├── modules/
│   ├── user_profiling.py         # Онбординг и профили
│   ├── personalization.py        # Персонализация ленты
│   ├── feedback_system.py        # Сбор фидбека
│   ├── crm_analytics.py          # CRM и сегментация
│   ├── rag_assistant.py          # AI-ассистент с RAG
│   └── reporting.py              # Генерация отчетов
│
├── bot/
│   ├── handlers_profile.py       # Хендлеры для профиля
│   ├── handlers_feed.py          # Персональная лента
│   ├── handlers_assistant.py     # AI-ассистент
│   └── handlers_admin_crm.py     # Админ-команды CRM
│
├── api/
│   └── miniapp.py                # Новые эндпоинты для аналитики
│
└── tasks/
    └── celery_tasks.py           # Новые задачи (отчеты, сегментация)
```

### 3.2. Новые таблицы БД

```sql
-- Профили пользователей
user_profiles (user_id, legal_interests, expertise_level, geo_interests, ...)

-- Фидбек от пользователей
user_feedback (user_id, publication_id, rating, issues, comment, ...)

-- Аналитика пользователей
user_analytics (user_id, total_sessions, lifecycle_stage, favorite_topics, ...)

-- Эмбеддинги для RAG
article_embeddings (publication_id, content_embedding, text_content, ...)

-- История взаимодействий
user_interactions (user_id, publication_id, action_type, timestamp, ...)
  -- action_type: 'view', 'reaction', 'share', 'save', 'feedback'
```

### 3.3. Интеграции

**Новые зависимости:**
- `pgvector` - векторный поиск в PostgreSQL
- `openpyxl` - экспорт в Excel
- `plotly` - интерактивные графики
- `scikit-learn` - ML для сегментации (опционально)

**API интеграции:**
- OpenAI Embeddings API (text-embedding-3-small)
- Существующие LLM API (GPT-4, Claude) для RAG

---

## 📊 4. Метрики успеха

### 4.1. KPI для отслеживания

**Вовлеченность:**
- ✅ Engagement rate > 5% (реакции / просмотры)
- ✅ Средняя длина сессии > 3 минуты
- ✅ Share rate > 2% (репосты / просмотры)

**Персонализация:**
- ✅ >70% пользователей завершают онбординг
- ✅ CTR на персонализированные статьи на 30% выше
- ✅ Churn rate < 5% в месяц

**Качество контента:**
- ✅ Средний рейтинг статей > 4.0/5.0
- ✅ <10% негативного фидбека
- ✅ >60% пользователей возвращаются в течение недели

**Аналитика:**
- ✅ Автоматические отчеты генерируются без ошибок
- ✅ Данные обновляются в real-time (<5 мин задержка)
- ✅ Export работает для датасетов до 10,000 строк

---

## 🚀 5. Приоритизация разработки

### Phase 1: Foundation (2-3 недели)
**Цель:** Базовая инфраструктура для персонализации

- [ ] Создать таблицы БД (user_profiles, user_analytics, user_interactions)
- [ ] Реализовать онбординг через FSM
- [ ] Базовое отслеживание взаимодействий (view, reaction)
- [ ] Команда `/settings_profile` для редактирования профиля
- [ ] Простая персонализация ленты (фильтр по топикам)

**Критерии готовности:**
- Пользователь может заполнить профиль
- Сохраняются взаимодействия в БД
- Базовая фильтрация работает

---

### Phase 2: Engagement (2-3 недели)
**Цель:** Повышение вовлеченности через фидбек

- [ ] Система сбора фидбека (inline-кнопки, опросы)
- [ ] CRM-аналитика (lifecycle стадии, сегменты)
- [ ] Автоматические триггеры (реактивация At Risk)
- [ ] Команды для админа (`/analytics_*`)
- [ ] Расширенная персонализация (ранжирование)

**Критерии готовности:**
- Собираем фидбек по статьям
- Автоматическая сегментация работает
- Админ видит CRM-метрики

---

### Phase 3: Intelligence (3-4 недели)
**Цель:** AI-ассистент и аналитика

- [ ] Установить pgvector, создать эмбеддинги
- [ ] RAG-система для поиска по архиву
- [ ] Команды `/search`, `/ask`, `/compare`
- [ ] Генерация автоматических дайджестов
- [ ] Адаптивное обучение профиля

**Критерии готовности:**
- Векторный поиск работает (<2 сек)
- AI отвечает на вопросы по архиву
- Дайджесты генерируются корректно

---

### Phase 4: Reporting (1-2 недели)
**Цель:** Автоматические отчеты и экспорт

- [ ] Еженедельные/месячные отчеты (Celery)
- [ ] Экспорт в Excel (`/export_stats`)
- [ ] Дашборд в Mini App (графики)
- [ ] Email-рассылка отчетов (опционально)

**Критерии готовности:**
- Отчеты генерируются по расписанию
- Excel-экспорт работает корректно
- Дашборд показывает актуальные данные

---

## 🔐 6. Безопасность и Privacy

### 6.1. Обработка персональных данных

**Принципы:**
- Минимизация данных (собираем только необходимое)
- Прозрачность (пользователь видит, что собирается)
- Контроль (может удалить данные через `/delete_my_data`)

**Реализация:**
- Хранить только обезличенные данные где возможно
- Шифрование sensitive полей (если будем собирать email)
- GDPR-совместимость (право на забвение)

**Команды:**
- `/privacy` - политика конфиденциальности
- `/my_data` - какие данные собраны обо мне
- `/delete_my_data` - удалить профиль и историю

---

## 📈 7. Потенциал монетизации (будущее)

### 7.1. Premium-функции

На основе CRM-аналитики можно предложить платные функции:

**Premium подписка ($5-10/месяц):**
- 📊 Расширенная персональная аналитика
- 🔍 Приоритетный доступ к новостям (раньше публикации)
- 🤖 Неограниченные запросы к AI-ассистенту
- 📥 Экспорт личного архива
- 🎯 Кастомные алерты по ключевым словам

**B2B для юрфирм ($50-100/месяц):**
- 👥 Командный доступ (до 10 юристов)
- 📊 Корпоративная аналитика
- 🔔 Алерты для конкретных клиентов/индустрий
- 📄 White-label дайджесты
- ☎️ Консультации по legal tech

**Affiliate:**
- Реферальная программа (приведи друга - получи месяц Premium)
- Партнерство с LegalTech SaaS

---

## ✅ 8. Критерии приемки

**Phase 1 считается завершенной, когда:**
- ✅ Новый пользователь проходит онбординг за <2 минут
- ✅ Профиль сохраняется в БД корректно
- ✅ Персонализация ленты работает (показывает только релевантные темы)
- ✅ 100% покрытие тестами для user_profiling модуля
- ✅ Документация обновлена

**Phase 2 считается завершенной, когда:**
- ✅ Фидбек собирается на >50% опубликованных статей
- ✅ Сегментация пользователей работает автоматически
- ✅ At Risk пользователи получают реактивационные сообщения
- ✅ Админ видит real-time CRM-метрики
- ✅ A/B тест показывает рост engagement rate на 10%+

**Phase 3 считается завершенной, когда:**
- ✅ Векторный поиск возвращает топ-5 релевантных статей за <2 сек
- ✅ AI-ассистент отвечает на вопросы с цитатами из архива
- ✅ Дайджесты генерируются корректно (проверка на 10 темах)
- ✅ >80% пользователей довольны качеством AI-ответов

**Phase 4 считается завершенной, когда:**
- ✅ Отчеты генерируются автоматически без ошибок
- ✅ Excel-файлы открываются корректно в Google Sheets/Excel
- ✅ Дашборд загружается за <3 секунды
- ✅ Отчет содержит все заявленные метрики

---

## 🎬 9. Начало разработки

**Статус:** 🟡 Ожидание команды на старт

**Когда начинаем код:**
- Ждем команды от пользователя: "Начинаем Phase 1"
- Пока фокус на Mini App (статистика - приоритет)

**Первый шаг при старте:**
1. Создать миграцию БД для новых таблиц
2. Реализовать FSM для онбординга
3. Базовый UI в боте для профиля
4. Тестирование на тестовом пользователе

---

## 📝 10. Связь с текущей системой

**Интеграция с существующим:**
- Используем существующую архитектуру (aiogram, FastAPI, Celery)
- Расширяем `app/bot/handlers.py` новыми хендлерами
- Дополняем Mini App новыми страницами
- Переиспользуем LLM-интеграции для RAG

**Не ломаем:**
- ✅ Текущий workflow сбора новостей
- ✅ Модерация через бот/Mini App
- ✅ Публикация в канал
- ✅ Существующие команды и меню

**Улучшаем:**
- 📈 Добавляем персонализацию поверх существующей системы
- 📊 Расширяем аналитику
- 🤖 Даем новые возможности через AI

---

**Готово к обсуждению!** 🎯
Жду команды вернуться к Mini App и исправить проблему с mock данными.
