# 🚀 Деплой Mini App на Vercel - Пошаговая инструкция

## Шаг 1: Подготовка репозитория

### 1.1 Убедитесь что изменения закоммичены

```bash
cd /home/user/Telegram_channel_auto
git status
git add mini-app/
git commit -m "fix: Prepare mini-app for Vercel deployment"
git push origin claude/test-telegram-news-api-Hm0TL
```

### 1.2 Опционально: Создайте отдельную ветку для деплоя

```bash
# Если хотите задеплоить с main ветки
git checkout main
git merge claude/test-telegram-news-api-Hm0TL
git push origin main
```

---

## Шаг 2: Создание проекта на Vercel

### 2.1 Регистрация на Vercel

1. Перейдите на https://vercel.com
2. Нажмите **"Sign Up"**
3. Выберите **"Continue with GitHub"**
4. Авторизуйте Vercel доступ к вашему GitHub

### 2.2 Импорт проекта

1. На дашборде Vercel нажмите **"Add New..."** → **"Project"**
2. Найдите репозиторий **`Telegram_channel_auto`**
3. Нажмите **"Import"**

### 2.3 Настройка проекта

**Root Directory:**
- Нажмите **"Edit"** рядом с Root Directory
- Введите: `mini-app`
- Это важно! Vercel должен знать что код в папке mini-app

**Framework Preset:**
- Vercel автоматически определит: **Next.js**
- Если нет - выберите вручную

**Build Settings:**
- Build Command: `npm run build` (автоматически)
- Output Directory: `.next` (автоматически)
- Install Command: `npm install` (автоматически)

---

## Шаг 3: Настройка Environment Variables

На странице настройки проекта в Vercel, в разделе **"Environment Variables"** добавьте:

### 3.1 Обязательные переменные

```bash
# Backend API URL (замените на ваш реальный URL)
NEXT_PUBLIC_API_URL=https://your-backend-domain.com

# Telegram Bot Username (без @)
NEXT_PUBLIC_BOT_USERNAME=your_bot_username

# Production URL (заполнится автоматически после первого деплоя)
NEXT_PUBLIC_BASE_URL=https://your-vercel-app.vercel.app
```

**Где взять значения:**

1. **NEXT_PUBLIC_API_URL**:
   - Если у вас уже есть production сервер: `https://api.yourdomain.com`
   - Если запущено локально: используйте ngrok (см. Шаг 4)
   - Формат: без `/` в конце

2. **NEXT_PUBLIC_BOT_USERNAME**:
   - Откройте @BotFather в Telegram
   - Найдите вашего бота
   - Скопируйте username (например: `legal_ai_news_bot`)

3. **NEXT_PUBLIC_BASE_URL**:
   - После первого деплоя Vercel даст вам URL вида `https://legal-ai-news-miniapp-xxx.vercel.app`
   - Скопируйте и добавьте как переменную
   - Или оставьте пустым на первом деплое и добавьте потом

### 3.2 Нажмите "Deploy"

---

## Шаг 4: Временное решение для локального backend (ngrok)

Если ваш backend запущен локально на Docker, используйте ngrok для временного доступа:

### 4.1 Установите ngrok

```bash
# Linux/Mac
wget https://bin.equinox.io/c/bNyj1mQVY4c/ngrok-v3-stable-linux-amd64.tgz
tar -xvzf ngrok-v3-stable-linux-amd64.tgz
sudo mv ngrok /usr/local/bin/
```

### 4.2 Запустите ngrok

```bash
# Регистрация (бесплатно)
ngrok authtoken YOUR_AUTH_TOKEN

# Запустите туннель к вашему FastAPI (порт 8000)
ngrok http 8000
```

### 4.3 Скопируйте URL

Ngrok выдаст URL вида: `https://abc123.ngrok.io`

Используйте его как `NEXT_PUBLIC_API_URL` в Vercel.

**⚠️ ВАЖНО:**
- ngrok URL меняется при каждом перезапуске (в бесплатной версии)
- Для production используйте настоящий домен

---

## Шаг 5: После успешного деплоя

### 5.1 Получите URL вашего Mini App

Vercel покажет URL вида:
```
https://legal-ai-news-miniapp-xxx.vercel.app
```

### 5.2 Настройте Telegram Bot

1. Откройте @BotFather в Telegram
2. Отправьте `/mybots`
3. Выберите вашего бота
4. Нажмите **"Bot Settings"** → **"Menu Button"**
5. Выберите **"Edit Menu Button URL"**
6. Вставьте URL вашего Vercel приложения
7. Вернитесь и выберите **"Edit Menu Button Text"**
8. Введите текст кнопки: **"📊 Аналитика"** или **"🚀 Dashboard"**

### 5.3 Обновите environment variable в Vercel

1. Зайдите в Settings → Environment Variables
2. Найдите `NEXT_PUBLIC_BASE_URL`
3. Обновите значение на реальный Vercel URL
4. Нажмите **"Save"**
5. Перейдите в **Deployments** → три точки → **"Redeploy"**

### 5.4 Добавьте MINI_APP_URL в основной проект

В вашем основном `.env` файле (где FastAPI):

```bash
MINI_APP_URL=https://legal-ai-news-miniapp-xxx.vercel.app
```

Перезапустите бота:
```bash
docker compose restart bot
```

---

## Шаг 6: Проверка работы

### 6.1 Откройте бота в Telegram

1. Найдите вашего бота
2. Нажмите кнопку меню (справа от поля ввода)
3. Должна появиться кнопка **"📊 Аналитика"** (или ваш текст)
4. Нажмите на неё

### 6.2 Mini App должен открыться

Если всё настроено правильно:
- ✅ Загрузится Dashboard с графиками
- ✅ Будут работать переходы между страницами
- ✅ Данные загрузятся с backend API

### 6.3 Troubleshooting

**Проблема: "Failed to fetch data"**
- Проверьте `NEXT_PUBLIC_API_URL` в Vercel
- Убедитесь что backend доступен (ngrok работает или production сервер запущен)
- Проверьте CORS настройки в FastAPI

**Проблема: "This page cannot be displayed"**
- Проверьте CSP headers в vercel.json
- Убедитесь что URL правильно добавлен в BotFather

**Проблема: Пустой экран**
- Откройте DevTools в Telegram Desktop (Ctrl+Shift+I)
- Посмотрите ошибки в консоли
- Проверьте Network tab - загружаются ли данные

---

## Шаг 7: Custom Domain (опционально)

### 7.1 Добавьте домен в Vercel

1. В настройках проекта → **Domains**
2. Нажмите **"Add"**
3. Введите ваш домен: `miniapp.yourdomain.com`

### 7.2 Настройте DNS

Добавьте CNAME запись в вашем DNS провайдере:
```
Type: CNAME
Name: miniapp
Value: cname.vercel-dns.com
```

### 7.3 Обновите URLs

После добавления домена:
- Обновите `NEXT_PUBLIC_BASE_URL` в Vercel
- Обновите Menu Button URL в BotFather
- Обновите `MINI_APP_URL` в основном проекте

---

## 📊 Проверка метрик

После успешного деплоя, в Vercel Dashboard вы увидите:
- Analytics: количество визитов
- Performance: скорость загрузки
- Logs: логи выполнения
- Deployments: история деплоев

---

## 🔄 Обновление приложения

При изменении кода:

```bash
# Закоммитьте изменения
git add mini-app/
git commit -m "feat: Add new feature"
git push origin main

# Vercel автоматически задеплоит обновление!
```

---

## ⚡ Быстрый чеклист

- [ ] Код залит на GitHub
- [ ] Проект импортирован в Vercel
- [ ] Root Directory установлена: `mini-app`
- [ ] Environment Variables добавлены
- [ ] Первый деплой выполнен успешно
- [ ] URL скопирован и добавлен в BotFather
- [ ] `NEXT_PUBLIC_BASE_URL` обновлена в Vercel
- [ ] `MINI_APP_URL` добавлена в основной .env
- [ ] Бот перезапущен
- [ ] Mini App открывается в Telegram
- [ ] Данные загружаются с backend

---

## 🎉 Готово!

Ваш Telegram Mini App теперь доступен пользователям бота!

**Следующие шаги:**
- Тестирование всех функций
- Мониторинг ошибок в Vercel Logs
- Добавление новых фич
- Настройка production backend (вместо ngrok)
