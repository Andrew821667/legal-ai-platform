# 🔧 Инструкция: Как открыть Debug Page

## Что было добавлено

✅ **Debug Page** - специальная страница `/debug` в Mini App, которая показывает диагностику **прямо в интерфейсе** (не нужна консоль браузера!)

---

## Как использовать

### Шаг 1: Создайте PR и дождитесь деплоя

1. Откройте: https://github.com/Andrew821667/Telegram_channel_auto/compare/main...claude/test-telegram-news-api-Hm0TL

2. Нажмите **"Create pull request"**

3. Merge PR

4. Подождите 2-3 минуты пока Vercel задеплоит

5. Проверьте в Vercel Dashboard что деплой успешен

---

### Шаг 2: Откройте Mini App

1. Откройте бота в Telegram: @legal_ai_news_moderator_bot

2. Нажмите кнопку Mini App (или команду для открытия)

3. На главной странице увидите кнопку **"🔧 Debug"**

---

### Шаг 3: Запустите диагностику

1. Нажмите кнопку **"🔧 Debug"**

2. На странице Debug нажмите большую кнопку: **"🚀 Run All Diagnostics"**

3. Подождите 5-10 секунд пока пройдут все тесты

4. Вы увидите результаты:
   - ✅ **Зеленые** - тесты прошли успешно
   - ❌ **Красные** - тесты упали (здесь проблема!)
   - ⏳ **Желтые** - тесты выполняются

---

### Шаг 4: Посмотрите детали ошибок

Для каждого теста с ❌ красным статусом:

1. Нажмите **"📊 Details"** чтобы развернуть детали

2. Вы увидите:
   - Код ошибки (например, 403, 404, 500)
   - Сообщение об ошибке
   - Данные от сервера
   - URL который был вызван

3. **Сделайте скриншот** результатов

---

## Что тестирует Debug Page

### Test 1: Environment
- Проверяет `NEXT_PUBLIC_API_URL` установлена ли
- Показывает `NODE_ENV` (должно быть `production`)
- Показывает текущий URL страницы

**Ожидаемый результат:**
```json
{
  "NEXT_PUBLIC_API_URL": "https://ronny-cholinergic-subcircularly.ngrok-free.dev",
  "NODE_ENV": "production"
}
```

**Если ошибка:**
- `NEXT_PUBLIC_API_URL: "NOT SET"` → env var не установлена в Vercel

---

### Test 2: Telegram
- Проверяет доступен ли Telegram WebApp API
- Показывает user_id, username, platform

**Ожидаемый результат:**
```json
{
  "user_id": 123456789,
  "username": "your_username",
  "platform": "ios" или "android" или "web"
}
```

**Если ошибка:**
- "Telegram WebApp not available" → Mini App открыт не в Telegram

---

### Test 3: API /dashboard/stats
- Тестирует эндпоинт статистики
- Должен вернуть: total_drafts, total_published

**Ожидаемый результат:**
```json
{
  "total_drafts": 3,
  "total_published": 59
}
```

**Если ошибка (примеры):**
- **Status 403** → проблема с авторизацией (Telegram auth)
- **Status 404** → неправильный URL или эндпоинт не найден
- **Status 500** → ошибка на сервере (проверьте логи Docker)
- **Network Error** → не может подключиться к API (проблема с ngrok?)
- **CORS error** → backend не разрешает requests с Vercel домена

---

### Test 4-6: API /drafts, /published, /settings
- Аналогично тестируют остальные эндпоинты
- Должны вернуть данные

---

## Типичные проблемы и решения

### Проблема: NEXT_PUBLIC_API_URL = "NOT SET"

**Решение:**
1. Зайдите в Vercel Dashboard
2. Settings → Environment Variables
3. Добавьте `NEXT_PUBLIC_API_URL` = `https://ronny-cholinergic-subcircularly.ngrok-free.dev`
4. **ВАЖНО:** выберите Environment = **Production**
5. Redeploy приложение

---

### Проблема: Status 403 Forbidden

**Причина:** Backend не принимает Telegram auth данные

**Временное решение (для теста):**
Отключить проверку auth в `app/api/miniapp.py`:

```python
async def verify_telegram_user(
    telegram_init_data: str = Header(None, alias="X-Telegram-Init-Data")
):
    # ВРЕМЕННО для теста - принимаем любого пользователя
    return {"user_id": 0, "username": "test"}

    # Закомментируйте остальной код валидации...
```

После этого перезапустите Docker контейнер:
```bash
docker compose restart app
```

---

### Проблема: Network Error / Cannot connect

**Причина:** Ngrok tunnel не работает или недоступен

**Проверка:**
1. Откройте в браузере: https://ronny-cholinergic-subcircularly.ngrok-free.dev/api/miniapp/dashboard/stats
2. Если не открывается → ngrok tunnel умер
3. Перезапустите ngrok:
   ```bash
   ngrok http 8000
   ```
4. Обновите NEXT_PUBLIC_API_URL в Vercel с новым ngrok URL

---

### Проблема: CORS Error

**Решение:**
Проверьте что в `app/main.py` стоит:

```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],  # Разрешаем все origins
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

---

## После того как все тесты ✅ зеленые

Если Debug Page показывает что все тесты проходят успешно:

1. Вернитесь на главную страницу (Dashboard)
2. Обновите страницу (pull down to refresh)
3. **Должны увидеть реальные данные:**
   - Черновики: 3
   - Опубликовано: 59
   - Просмотры: реальное число
   - Реакции: реальное число

4. Зайдите в "Модерация контента" → должны увидеть 3 реальных черновика
5. Зайдите в "Опубликованное" → должны увидеть 59 статей

---

## Если что-то не работает

1. **Сделайте скриншот Debug Page** с результатами всех тестов
2. Сделайте скриншот **Details** для красных тестов
3. Отправьте мне скриншоты
4. Я скажу что именно нужно исправить!

---

**Готово к тестированию!** 🎯

Следующий шаг: Создайте PR, дождитесь деплоя, откройте /debug page!
