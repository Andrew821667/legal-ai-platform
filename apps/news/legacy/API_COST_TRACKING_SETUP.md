# 💰 API Cost Tracking - Руководство по настройке

> **✨ Обновление (2025-12-28):** Теперь таблицы создаются **автоматически** при запуске!
> SQL миграция **НЕ НУЖНА** - просто перезапустите сервисы.

## ✅ ЧТО СДЕЛАНО

### 1. **Модели базы данных** (app/models/database.py)
- ✅ Создана модель `APIUsage` для детального отслеживания каждого запроса
- ✅ Создана модель `MonthlyAPIStats` для агрегированной месячной статистики

### 2. **Модуль отслеживания** (app/modules/api_usage_tracker.py)
- ✅ Функция `calculate_cost()` - расчёт стоимости по токенам
- ✅ Функция `track_api_usage()` - запись использования в БД
- ✅ Функция `get_current_month_cost()` - получение месячной статистики
- ✅ Актуальные цены (декабрь 2024):
  - GPT-4o: $2.50/$10.00 за 1M токенов (prompt/completion)
  - GPT-4o-mini: $0.15/$0.60 за 1M токенов
  - Perplexity Sonar: $0.20/$0.20 за 1M токенов

### 3. **Интеграция с LLM провайдерами** (app/modules/llm_provider.py)
- ✅ OpenAI: отслеживание токенов через `response.usage`
- ✅ Perplexity: отслеживание токенов через `response.usage`
- ✅ Автоматический вызов `track_api_usage()` после каждого запроса
- ✅ Поддержка параметров: operation, article_id, draft_id

### 4. **Отображение в боте** (app/bot/handlers.py)
- ✅ Обновлена функция `get_statistics()` в команде `/stats`
- ✅ Отображение:
  - Общая стоимость за текущий месяц
  - Количество токенов и запросов
  - Breakdown по провайдерам (OpenAI/Perplexity)

### 5. **SQL миграция** (migrations/add_api_usage_tracking.sql)
- ✅ Создание таблицы `api_usage`
- ✅ Создание таблицы `monthly_api_stats`
- ✅ Индексы для оптимизации запросов
- ✅ Комментарии к полям

### 6. **Автоматическая инициализация БД**
- ✅ Bot (`app/bot/handlers.py`): вызов `init_db()` в `start_bot()`
- ✅ Celery Worker (`app/tasks/celery_tasks.py`): startup hook создаёт таблицы
- ✅ FastAPI (`app/main.py`): `init_db()` в lifespan
- ✅ Все таблицы создаются автоматически через SQLAlchemy metadata
- ✅ Идемпотентность - безопасно запускать многократно

---

## 🚀 СЛЕДУЮЩИЕ ШАГИ

### Шаг 1: Перезапустить сервисы

**✨ ВАЖНО:** Таблицы теперь создаются **автоматически** при запуске любого сервиса!
SQL миграция **НЕ НУЖНА** - всё работает "из коробки".

**Просто перезапустите контейнеры:**

```bash
# Перезапустить все контейнеры для применения изменений кода
docker compose restart celery_worker bot app

# Проверить логи - должны увидеть "database_initialized"
docker compose logs celery_worker bot | grep database_initialized

# Проверить что таблицы созданы
docker compose exec postgres psql -U legal_user -d legal_ai_news -c "\dt" | grep -E "(api_usage|monthly_api_stats)"

# Должно вывести:
# public | api_usage         | table | legal_user
# public | monthly_api_stats | table | legal_user
```

---

### Шаг 2: Протестировать отслеживание

#### 2.1 Запустить сбор новостей

```bash
# В Telegram боте отправьте команду:
/fetch
```

Это запустит сбор новостей и генерацию драфтов, что создаст записи об использовании API.

#### 2.2 Проверить что данные сохранились

```bash
# Проверить записи в api_usage
docker compose exec postgres psql -U legal_user -d legal_ai_news -c "SELECT provider, model, operation, total_tokens, cost_usd FROM api_usage ORDER BY created_at DESC LIMIT 5;"

# Проверить месячную статистику
docker compose exec postgres psql -U legal_user -d legal_ai_news -c "SELECT * FROM monthly_api_stats ORDER BY year DESC, month DESC LIMIT 3;"
```

#### 2.3 Проверить отображение в боте

```bash
# В Telegram боте отправьте команду:
/stats
```

**Ожидаемый результат:**

```
📊 Статистика системы

📰 Всего статей: 123
📝 Всего драфтов: 45
✅ Опубликовано: 12
⏳ Ожидают модерации: 3

━━━━━━━━━━━━━━━━

💰 Стоимость API за December 2025

💵 Общая стоимость: $0.1234
📊 Всего токенов: 45,678
🔢 Всего запросов: 25

По провайдерам:
├─ OpenAI:
│  ├─ Стоимость: $0.0890
│  ├─ Токенов: 30,000
│  └─ Запросов: 15
├─ Perplexity:
│  ├─ Стоимость: $0.0344
│  ├─ Токенов: 15,678
│  └─ Запросов: 10

📅 Обновлено: 28.12.2025 20:45
```

---

## 🔍 Диагностика проблем

### Проблема: Таблицы не создаются

```bash
# Проверить права пользователя
docker compose exec postgres psql -U legal_user -d legal_ai_news -c "\du"

# Проверить подключение к БД
docker compose exec postgres psql -U legal_user -d legal_ai_news -c "SELECT current_database();"
```

### Проблема: Стоимость показывает $0.0000

```bash
# Проверить что есть записи в api_usage
docker compose exec postgres psql -U legal_user -d legal_ai_news -c "SELECT COUNT(*) FROM api_usage WHERE date >= CURRENT_DATE - INTERVAL '30 days';"

# Если 0 - значит сбор новостей не запускался или не было API вызовов
# Запустите /fetch в боте
```

### Проблема: Ошибка импорта модуля

```bash
# Проверить что файл api_usage_tracker.py на месте
ls -la app/modules/api_usage_tracker.py

# Перезапустить контейнеры
docker compose restart celery_worker bot app
```

---

## 📊 Как работает отслеживание

### 1. **Запись использования**

Каждый раз когда вызывается LLM API (OpenAI или Perplexity):

```python
# В app/modules/llm_provider.py
response = await client.chat.completions.create(...)

# Автоматически записывается в БД:
await track_api_usage(
    db=db,
    provider="openai",  # или "perplexity"
    model="gpt-4o-mini",
    operation="draft_generation",  # или "ranking", "editing", etc.
    prompt_tokens=response.usage.prompt_tokens,
    completion_tokens=response.usage.completion_tokens,
    article_id=article_id,
    draft_id=draft_id
)
```

### 2. **Расчёт стоимости**

```python
# Пример для GPT-4o-mini:
prompt_tokens = 1000
completion_tokens = 500

# Стоимость = (1000 / 1_000_000) * $0.15 + (500 / 1_000_000) * $0.60
# Стоимость = $0.00015 + $0.0003 = $0.00045
```

### 3. **Месячная агрегация**

После каждой записи обновляется `monthly_api_stats`:

```python
# Увеличиваются счётчики:
- total_requests += 1
- total_tokens += (prompt_tokens + completion_tokens)
- total_cost_usd += calculated_cost
```

### 4. **Отображение**

При вызове `/stats`:

```python
# Запрос к monthly_api_stats за текущий месяц
api_cost_data = await get_current_month_cost(db)

# Формирование ответа с детальной статистикой
```

---

## 📝 Обслуживание

### Архивация старых данных

```sql
-- Удалить детальные записи старше 6 месяцев (месячная статистика сохранится)
DELETE FROM api_usage
WHERE date < CURRENT_DATE - INTERVAL '6 months';
```

### Обновление цен

Если цены на API изменились, обновите файл `app/modules/api_usage_tracker.py`:

```python
PRICING = {
    "openai": {
        "gpt-4o-mini": {
            "prompt": 0.150,     # Обновите здесь
            "completion": 0.600  # И здесь
        }
    }
}
```

### Экспорт данных

```bash
# Экспорт всех данных об использовании API
docker compose exec postgres psql -U legal_user -d legal_ai_news -c "COPY (SELECT * FROM api_usage) TO STDOUT WITH CSV HEADER" > api_usage_export.csv

# Экспорт месячной статистики
docker compose exec postgres psql -U legal_user -d legal_ai_new -c "COPY (SELECT * FROM monthly_api_stats) TO STDOUT WITH CSV HEADER" > monthly_stats_export.csv
```

---

## 🎯 Примеры запросов

### Самые дорогие операции

```sql
SELECT
    operation,
    COUNT(*) as requests,
    SUM(total_tokens) as total_tokens,
    SUM(cost_usd) as total_cost_usd,
    AVG(cost_usd) as avg_cost_per_request
FROM api_usage
WHERE date >= CURRENT_DATE - INTERVAL '30 days'
GROUP BY operation
ORDER BY total_cost_usd DESC;
```

### Статистика по моделям

```sql
SELECT
    provider,
    model,
    COUNT(*) as requests,
    SUM(cost_usd) as total_cost
FROM api_usage
WHERE date >= CURRENT_DATE - INTERVAL '30 days'
GROUP BY provider, model
ORDER BY total_cost DESC;
```

### Динамика расходов по дням

```sql
SELECT
    date,
    COUNT(*) as requests,
    SUM(total_tokens) as tokens,
    SUM(cost_usd) as cost_usd
FROM api_usage
WHERE date >= CURRENT_DATE - INTERVAL '30 days'
GROUP BY date
ORDER BY date DESC;
```

---

## 📞 Проблемы и решения

### Если процесс сбора "завис"

Пользователь упомянул "процесс завис". Проверьте:

1. **Логи Celery worker:**
   ```bash
   docker compose logs -f celery_worker | tail -100
   ```

2. **Проверить задачи в очереди:**
   ```bash
   docker compose exec celery_worker celery -A app.tasks.celery_tasks inspect active
   ```

3. **Перезапустить worker:**
   ```bash
   docker compose restart celery_worker
   ```

4. **Проверить подключение к Telegram API:**
   ```bash
   # Проверить session файл
   ls -la telegram_bot.session

   # Попробовать переавторизацию
   python setup_telegram_session.py
   ```

5. **Проверить Redis (если используется как брокер):**
   ```bash
   docker compose exec redis redis-cli ping
   # Должно вернуть: PONG
   ```

---

## ✅ Checklist завершения

- [ ] SQL миграция применена успешно
- [ ] Таблицы `api_usage` и `monthly_api_stats` созданы
- [ ] Контейнеры перезапущены
- [ ] Команда `/fetch` выполнена без ошибок
- [ ] В БД появились записи в `api_usage`
- [ ] Команда `/stats` показывает стоимость API
- [ ] Процесс сбора не зависает

---

**Версия:** 1.0.0
**Дата:** 2025-12-28
**Статус:** ✅ Code Complete, ⏳ Testing Required
**Коммит:** c489ee0
