# 🚀 БЫСТРЫЙ СТАРТ - Contract AI System

## Запуск на вашем компьютере за 5 минут

### 📋 Требования
- Python 3.11 или выше
- Git
- 5-10 минут времени

---

## 🏃 ВАРИАНТ 1: Упрощенный запуск (РЕКОМЕНДУЕТСЯ)

### Шаг 1: Скачайте проект

```bash
# Клонируйте репозиторий
git clone <URL_вашего_репозитория>
cd Contract-AI-System-
```

### Шаг 2: Создайте виртуальное окружение

```bash
# macOS/Linux:
python3 -m venv venv
source venv/bin/activate

# Windows:
python -m venv venv
venv\Scripts\activate
```

### Шаг 3: Установите ТОЛЬКО необходимые зависимости

```bash
# Базовые зависимости для запуска demo
pip install fastapi uvicorn python-dotenv python-multipart
```

### Шаг 4: Запустите demo сервер

```bash
python3 start_demo.py
```

### Шаг 5: Откройте в браузере

```
http://localhost:8000/docs
```

**Готово!** 🎉 Сервер работает на вашем компьютере!

---

## 🔧 ВАРИАНТ 2: Полная установка (с аутентификацией)

### Шаг 1-2: Такие же, как выше

### Шаг 3: Установите ВСЕ зависимости

```bash
pip install --upgrade pip
pip install -r requirements.txt
```

⚠️ **Примечание:** Если возникнут ошибки с `cryptography`, попробуйте:

```bash
# macOS/Linux:
sudo apt-get update
sudo apt-get install build-essential libssl-dev libffi-dev python3-dev

# macOS:
brew install openssl

# Затем:
pip install --no-cache-dir -r requirements.txt
```

### Шаг 4: Проверьте .env файл

Файл `.env` уже создан с настройками для разработки. При необходимости измените:

```bash
# Откройте .env и добавьте ваши ключи:
OPENAI_API_KEY=sk-your-key-here
STRIPE_SECRET_KEY=sk_test_your-key-here
```

### Шаг 5: Запустите полный сервер

```bash
python3 -m uvicorn src.main:app --host 0.0.0.0 --port 8000 --reload
```

### Шаг 6: Откройте в браузере

```
http://localhost:8000/docs
```

**Готово!** 🎉 Полная система работает!

---

## 🐳 ВАРИАНТ 3: Docker (если установлен)

### Шаг 1: Соберите образ

```bash
docker build -t contract-ai .
```

### Шаг 2: Запустите контейнер

```bash
docker run -p 8000:8000 --env-file .env contract-ai
```

### Шаг 3: Откройте в браузере

```
http://localhost:8000/docs
```

---

## ✅ Как проверить, что всё работает?

После запуска откройте в браузере:

1. **Swagger UI** - http://localhost:8000/docs
   - Интерактивная документация
   - Можно тестировать API прямо в браузере

2. **ReDoc** - http://localhost:8000/redoc
   - Красивая документация API

3. **Health Check** - http://localhost:8000/health
   - Должен вернуть `{"status": "healthy"}`

---

## 🎯 Доступные эндпоинты

### Demo режим (start_demo.py):
- ✅ GET `/` - Информация об API
- ✅ GET `/health` - Проверка здоровья сервера
- ✅ GET `/api/v1/info` - Список функций
- ✅ GET `/docs` - Swagger UI
- ✅ GET `/redoc` - ReDoc

### Полный режим (src.main:app):
- ✅ **Аутентификация** (8 эндпоинтов)
  - POST `/api/v1/auth/register`
  - POST `/api/v1/auth/login`
  - POST `/api/v1/auth/activate-demo`
  - И другие...

- ✅ **Контракты** (8 эндпоинтов)
  - POST `/api/v1/contracts/upload`
  - POST `/api/v1/contracts/analyze`
  - POST `/api/v1/contracts/generate`
  - GET `/api/v1/contracts/list`
  - И другие...

- ✅ **WebSocket** (2 эндпоинта)
  - WS `/api/v1/ws/analysis/{contract_id}`
  - WS `/api/v1/ws/notifications`

- ✅ **Платежи** (6 эндпоинтов)
  - GET `/api/v1/payments/pricing`
  - POST `/api/v1/payments/checkout`
  - POST `/api/v1/payments/webhooks/stripe`
  - И другие...

---

## 🔧 Решение проблем

### Проблема: "Port 8000 already in use"

```bash
# Найдите процесс
lsof -i :8000  # macOS/Linux
netstat -ano | findstr :8000  # Windows

# Остановите процесс
kill -9 <PID>  # macOS/Linux
taskkill /PID <PID> /F  # Windows
```

### Проблема: "Module not found"

```bash
# Убедитесь, что виртуальное окружение активировано
# Должно быть (venv) в начале строки терминала

# Переустановите зависимости
pip install --upgrade -r requirements.txt
```

### Проблема: "Permission denied"

```bash
# Linux:
chmod +x start_demo.py

# Или запустите через python:
python3 start_demo.py
```

---

## 📱 Тестирование API через curl

```bash
# Health check
curl http://localhost:8000/health

# API info
curl http://localhost:8000/api/v1/info

# Регистрация (полный режим)
curl -X POST http://localhost:8000/api/v1/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "password": "TestPass123!",
    "full_name": "Test User"
  }'
```

---

## 🎉 Что делать дальше?

### 1. Изучите API в Swagger UI
- Откройте http://localhost:8000/docs
- Попробуйте эндпоинты
- Посмотрите схемы данных

### 2. Настройте API ключи
Отредактируйте `.env`:
```bash
OPENAI_API_KEY=sk-your-real-key
STRIPE_SECRET_KEY=sk_test_your-real-key
SENDGRID_API_KEY=SG.your-real-key
```

### 3. Запустите frontend (опционально)
```bash
cd frontend
npm install
npm run dev
# Откройте http://localhost:3000
```

### 4. Запустите тесты
```bash
python3 test_full_system.py
```

---

## 📞 Нужна помощь?

Если что-то не работает:

1. Проверьте версию Python: `python3 --version` (нужна 3.11+)
2. Проверьте, что виртуальное окружение активировано
3. Посмотрите логи: `tail -f logs/demo_server.log`
4. Проверьте порт: `lsof -i :8000`

---

**Успешного запуска!** 🚀

Вся система готова к работе и ждёт только запуска на вашем компьютере.
