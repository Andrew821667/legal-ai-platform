# 🖥️ Кнопки запуска/остановки на рабочем столе

## 📦 Быстрая установка

**Выполните ОДИН раз в терминале:**

```bash
cd ~/Desktop/Contract-AI-System-
./create_desktop_shortcuts.sh
```

После этого на вашем рабочем столе появятся 2 кнопки:
- 🚀 **Запустить Contract AI.command** - запускает админ-панель
- ⏹️ **Остановить Contract AI.command** - останавливает админ-панель

---

## 🚀 Использование

### Запуск админ-панели:
1. **Двойной клик** по файлу `Запустить Contract AI.command` на рабочем столе
2. При первом запуске macOS попросит разрешение → нажмите **"Открыть"**
3. Откроется терминал, админ-панель запустится автоматически
4. Браузер откроется по адресу: http://localhost:8501

### Остановка админ-панели:
1. **Двойной клик** по файлу `Остановить Contract AI.command` на рабочем столе
2. Все процессы Streamlit будут остановлены
3. Окно терминала закроется автоматически

---

## ⚙️ Альтернативные способы

### Способ 1: Прямой запуск из терминала
```bash
cd ~/Desktop/Contract-AI-System-
./start_admin_panel.command
```

### Способ 2: Через Python напрямую
```bash
cd ~/Desktop/Contract-AI-System-
source venv/bin/activate
streamlit run admin/streamlit_dashboard.py
```

---

## 🔧 Устранение проблем

### Проблема: "Невозможно открыть, файл не проверен"

**Решение 1** (рекомендуется):
1. Правой кнопкой мыши на файл .command
2. Выберите **"Открыть"** (не двойной клик!)
3. Нажмите **"Открыть"** в диалоге

**Решение 2**:
1. Системные настройки → Конфиденциальность и безопасность
2. Найдите предупреждение о заблокированном файле
3. Нажмите **"Разрешить в любом случае"**

### Проблема: Скрипт не запускается

**Проверьте права:**
```bash
cd ~/Desktop/Contract-AI-System-
chmod +x start_admin_panel.command
chmod +x stop_admin_panel.command
```

### Проблема: "OPENAI_API_KEY не найден"

**Решение:**
1. Убедитесь, что файл `.env` существует в корне проекта
2. Откройте `.env` и проверьте наличие строки:
   ```
   OPENAI_API_KEY=sk-...ваш_ключ...
   ```

---

## 📝 Что делают скрипты

### `start_admin_panel.command`:
1. ✅ Активирует виртуальное окружение Python
2. ✅ Загружает переменные из `.env`
3. ✅ Запускает Streamlit админ-панель
4. ✅ Открывает браузер автоматически

### `stop_admin_panel.command`:
1. ✅ Находит все процессы Streamlit
2. ✅ Корректно останавливает процессы
3. ✅ Принудительно завершает зависшие процессы (если нужно)

### `create_desktop_shortcuts.sh`:
1. ✅ Создает ярлыки на рабочем столе
2. ✅ Прописывает правильные пути к проекту
3. ✅ Устанавливает права на выполнение

---

## 🎯 Дополнительные возможности

### Создать ярлык в Dock (панель внизу экрана):
1. Запустите `Запустить Contract AI.command`
2. Иконка появится в Dock
3. Правой кнопкой → Параметры → **"Оставить в Dock"**

### Изменить иконку кнопки:
1. Найдите красивую иконку (.icns файл)
2. Правой кнопкой на .command → Свойства
3. Перетащите иконку на маленькую иконку вверху окна

---

## 🔗 Полезные ссылки

- 🌐 Админ-панель: http://localhost:8501
- 📖 Документация: `docs/ARCHITECTURE.md`
- 🐛 Сообщить о проблеме: GitHub Issues

---

**✅ Готово! Теперь у вас есть удобные кнопки для запуска системы!**
